import { ContractFactory, Signer } from "ethers";
import { Provider } from "ethers/providers";
import { UnsignedTransaction } from "ethers/utils/transaction";
import { TransactionOverrides } from ".";
import { TitleEscrowCreator } from "./TitleEscrowCreator";
export declare class TitleEscrowCreatorFactory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(overrides?: TransactionOverrides): Promise<TitleEscrowCreator>;
    getDeployTransaction(overrides?: TransactionOverrides): UnsignedTransaction;
    attach(address: string): TitleEscrowCreator;
    connect(signer: Signer): TitleEscrowCreatorFactory;
    static connect(address: string, signerOrProvider: Signer | Provider): TitleEscrowCreator;
}
