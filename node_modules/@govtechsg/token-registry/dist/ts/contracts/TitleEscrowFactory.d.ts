import { ContractFactory, Signer } from "ethers";
import { Provider } from "ethers/providers";
import { UnsignedTransaction } from "ethers/utils/transaction";
import { TransactionOverrides } from ".";
import { TitleEscrow } from "./TitleEscrow";
export declare class TitleEscrowFactory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(_tokenRegistry: string, _beneficiary: string, _holder: string, _titleEscrowFactoryAddress: string, overrides?: TransactionOverrides): Promise<TitleEscrow>;
    getDeployTransaction(_tokenRegistry: string, _beneficiary: string, _holder: string, _titleEscrowFactoryAddress: string, overrides?: TransactionOverrides): UnsignedTransaction;
    attach(address: string): TitleEscrow;
    connect(signer: Signer): TitleEscrowFactory;
    static connect(address: string, signerOrProvider: Signer | Provider): TitleEscrow;
}
