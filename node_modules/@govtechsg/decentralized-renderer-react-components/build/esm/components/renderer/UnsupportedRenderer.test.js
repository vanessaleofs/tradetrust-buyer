import React from "react";
import { render } from "@testing-library/react";
import { UnsupportedRenderer } from "./UnsupportedRenderer";
describe("component UnsupportedRenderer", function () {
    it("must display mime-type not supported", function () {
        var attachment = { type: "application/epub+zip" };
        // eslint-disable-next-line @typescript-eslint/ban-ts-ignore
        // @ts-ignore this attachment is not handled by the lib
        var getByText = render(React.createElement(UnsupportedRenderer, { attachment: attachment })).getByText;
        expect(getByText("Rendering of this type of attachment is not supported.")).toBeDefined();
        expect(getByText("Please check your mime-type: application/epub+zip")).toBeDefined();
    });
});
//# sourceMappingURL=UnsupportedRenderer.test.js.map