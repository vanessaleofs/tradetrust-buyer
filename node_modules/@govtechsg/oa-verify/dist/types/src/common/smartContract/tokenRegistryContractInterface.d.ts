import { Contract } from "ethers";
import { v2, v3, WrappedDocument } from "@govtechsg/open-attestation";
export declare const getIssuersTokenRegistry: (document: WrappedDocument<v3.OpenAttestationDocument> | WrappedDocument<v2.OpenAttestationDocument>) => string[];
export declare const createTokenRegistryContract: (address: string, { network }: {
    network: string;
}) => Contract;
export declare const isMintedOnTokenRegistry: (smartContract: Contract, hash: string) => Promise<boolean>;
