import * as ethers from "ethers";
import { Hash } from "../../types/core";
interface ContractInstance {
    abi: any;
    network: string;
    contractAddress: Hash;
}
export declare const getProvider: (options: {
    network: string;
}) => ethers.ethers.providers.Provider;
export declare const contractInstance: (options: ContractInstance) => ethers.ethers.Contract;
export {};
