import { v2, v3, WrappedDocument } from "@govtechsg/open-attestation";
import { Contract } from "ethers";
import { Verifier } from "../../types/core";
export declare const isAnyHashRevoked: (smartContract: Contract, intermediateHashes: string[]) => Promise<string | undefined>;
export declare const openAttestationEthereumDocumentStoreRevoked: Verifier<WrappedDocument<v2.OpenAttestationDocument> | WrappedDocument<v3.OpenAttestationDocument>>;
