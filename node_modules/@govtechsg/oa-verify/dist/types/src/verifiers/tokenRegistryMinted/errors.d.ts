import { EthersError, Reason } from "../../types/error";
export declare const contractNotMinted: (merkleRoot: string, address: string) => Reason;
export declare const getErrorReason: (error: EthersError, address: string, hash: string) => Reason;
